export enum StorageField {
  EMAIL = 'email'
}

export enum Validity {
  EXPIRED = 'expired',
  INVALID = 'invalid',
  VALID = 'valid'
}

export enum LookupEmail {
  NOT_FOUND = 'not found',
  NOT_CLAIMED = 'not claimed',
  VERIFIED = 'verified'
}
